import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";

const roles = { top: { name: "TOP", title: "トップレーン攻略", lead: "1対1で経験値とゴールドを集め、サイドレーンからチームを支えるロールです。", basics: ["ミニオンを最後の一撃で倒してゴールドを集める", "ワードを置いてジャングラーの接近に備える", "テレポートは集団戦やオブジェクトに合わせて使う"] }, jungle: { name: "JG", title: "ジャングル攻略", lead: "マップを回りながら味方を支援し、試合の流れをつくるロールです。", basics: ["最初の周回ルートを決めてから試合を始める", "敵の位置が見えないレーンには無理に入らない", "ドラゴンやバロンは味方と一緒に確保する"] }, mid: { name: "MID", title: "ミッドレーン攻略", lead: "中央レーンからレーン戦とロームでチームに影響を与えるロールです。", basics: ["ウェーブを押したら視界を取りに行く", "敵ミッドの移動を味方に知らせる", "中央では敵ジャングラーの位置を常に警戒する"] }, adc: { name: "ADC", title: "ADC攻略", lead: "継続ダメージの中心として、集団戦で安全に火力を出すロールです。", basics: ["序盤はCSを安定して取ることを優先する", "敵のスキルが使われてから攻撃を始める", "味方の後ろから攻撃できる位置を保つ"] }, sup: { name: "SUP", title: "サポート攻略", lead: "視界とスキルで味方を支え、チームの戦いやすさを作るロールです。", basics: ["川とオブジェクト周辺に視界を置く", "ADCが安全にCSを取れるようにする", "集団戦では味方を守る相手を優先する"] } } as const;
export function generateStaticParams() { return Object.keys(roles).map((slug) => ({ slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const role = roles[slug as keyof typeof roles]; return role ? { title: role.title, description: role.lead } : {}; }
export default async function RolePage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const role = roles[slug as keyof typeof roles]; if (!role) notFound(); return <main><header className="site-header"><Link className="brand" href="/"><span>RIFT</span> NOTE</Link></header><section className="inner-hero"><p className="breadcrumb"><Link href="/">ホーム</Link><span>›</span><Link href="/roles">ロール別攻略</Link><span>›</span>{role.name}</p><p className="eyebrow">ROLE GUIDE</p><h1>{role.title}</h1><p>{role.lead}</p></section><section className="section list-page"><h2>まず意識したいこと</h2><div className="content-list">{role.basics.map((basic, index) => <div key={basic}><span>0{index + 1}</span><strong>{basic}</strong></div>)}</div><div className="detail-cta"><p>次に読む</p><Link href="/beginner">初心者ガイドを読む →</Link></div></section></main>; }
