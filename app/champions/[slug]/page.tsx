import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { champions } from "../../../content/site";

export function generateStaticParams() { return champions.map(({ slug }) => ({ slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const champion = champions.find((item) => item.slug === slug); return champion ? { title: `${champion.name}の攻略`, description: `${champion.name}の基本的な立ち回り、ビルド、集団戦での役割を解説。`, alternates: { canonical: `/champions/${slug}` } } : {}; }
export default async function ChampionPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const champion = champions.find((item) => item.slug === slug); if (!champion) notFound(); return <main><header className="site-header"><Link className="brand" href="/"><span>RIFT</span> NOTE</Link></header><section className="inner-hero champion-hero"><p className="breadcrumb"><Link href="/">ホーム</Link><span>›</span><Link href="/champions">チャンピオン</Link><span>›</span>{champion.name}</p><p className="eyebrow">{champion.role} / {champion.tag}</p><h1>{champion.name} 攻略</h1><p>{champion.en}の基本的な立ち回りを、初めて使う人にもわかりやすく解説します。</p></section><section className="section detail-layout"><aside><p>目次</p><a href="#build">おすすめビルド</a><a href="#lane">レーニング</a><a href="#teamfight">集団戦</a></aside><article className="detail-body"><h2 id="build">おすすめビルド</h2><p>序盤はレーンで安定して戦えるアイテムを選び、中盤以降はチームに足りない火力や耐久力を補いましょう。ビルドは相手チームの構成に応じて調整することが大切です。</p><h2 id="lane">レーニング</h2><p>序盤はミニオンを安定して倒し、相手のスキルが使われたタイミングで短いトレードを狙いましょう。視界がない状態でレーンを押し込みすぎないことも重要です。</p><h2 id="teamfight">集団戦</h2><p>自分の役割を確認し、無理に前へ出すぎないことを意識します。重要なスキルを使う相手を狙い、味方と同じ対象を攻撃しましょう。</p><div className="detail-cta"><p>関連記事</p><Link href="/articles/ahri-guide">チャンピオンの立ち回りとコンボを読む →</Link></div></article></section></main>; }
